time=10;
sample_frequency= 10000;  % in Hz
y0=[0;0];
tspan=0:(1/sample_frequency):time; 
ft=tspan;
f= 3.8*sin(tspan);



%[t,y]=ode5(@(t,y)pend_test(t,y,ft,f),tspan,y0);
y=ode5(@(t,y)pend_test(t,y,ft,f),tspan,y0);
theta=y(:,1);
plot(theta)
hold on;
theta_dot=y(:,2);
plot(theta_dot)
grid on
